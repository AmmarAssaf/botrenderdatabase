services:
  - type: web
    name: media-promotion-bot-free
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - key: BOT_TOKEN
        value: "8415474087:AAEDtwjvgogXfvpMzARe875svIEkSSDdNXk"
      - key: DATABASE_URL
        fromDatabase:
          name: media-promotion-db
          property: connectionString
      - key: RENDER_SERVICE_URL
        value: "https://media-promotion-bot-free.onrender.com"
      - key: PYTHON_VERSION
        value: "3.11.0"

databases:
  - name: media-promotion-db
    plan: free
    databaseName: media_promotion
    user: media_promotion_user
