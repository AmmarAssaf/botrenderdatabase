services:
  - type: web
    name: telegram-bot-db-free
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python simple_demo.py"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: telegram-bot-db
          property: connectionString

databases:
  - name: telegram-bot-db
    plan: free
